const MENU_HEIGHT = 64

const styles = {
  contentStyle: {
    padding: 0, 
    marginTop: MENU_HEIGHT, 
    // zIndex: 0,
  },
  breedcrumbStyle: {
    margin: 16
  },
  menuStyle: {
    lineHeight: `${MENU_HEIGHT}px`,
    zIndex: 501,
  },
  headerStyle: {
    position: 'fixed', 
    width: '100%',
    zIndex: 500,
  }
}

export default styles