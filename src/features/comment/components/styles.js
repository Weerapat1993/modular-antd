
const styles = {
  marginLeft: (value) => ({
    marginLeft: value
  }),
  marginHorizontal: (value) => ({
    marginHorizontal: value
  }),
  paddingHorizontal: (value) => ({
    paddingHorizontal: value
  }),
  backgroundColor: (value) => ({
    backgroundColor: value
  }),
  color: (value) => ({
    color: value
  }),
  maxWidth: (value) => ({
    maxWidth: value
  })
}

export default styles
